import { defineComponent, h } from "vue";

export const ImmutableGame = defineComponent(
  (props, { emit }) => {
    return () => h("TODO: Game component here");
  },
  {
    props: {},
    emits: {},
  },
);
